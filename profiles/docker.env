# Docker appliance profile — installs and enables Docker.
DOCKER_ENABLE="yes"

# Profile-specific additions (docker itself is installed via DOCKER_ENABLE path)
PROFILE_PACKAGES=""
PROFILE_SERVICES=""
